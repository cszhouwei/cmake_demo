INCLUDE_DIRECTORIES("${EXTLIB_INSTALL_PREFIX}/include")
INCLUDE_DIRECTORIES("${CMAKE_CURRENT_SOURCE_DIR}/libattrapi")

INCLUDE_DIRECTORIES("${CMAKE_CURRENT_SOURCE_DIR}/libl5_api")
ADD_LIBRARY(l5_api STATIC IMPORTED)
IF(USE_32BIT)
    SET_TARGET_PROPERTIES(l5_api PROPERTIES
        IMPORTED_LOCATION_DEBUG ${CMAKE_CURRENT_SOURCE_DIR}/libl5_api/libqos_client32.a
        IMPORTED_LOCATION_RELEASE ${CMAKE_CURRENT_SOURCE_DIR}/libl5_api/libqos_client32.a)
ELSE()
    SET_TARGET_PROPERTIES(l5_api PROPERTIES
        IMPORTED_LOCATION_DEBUG ${CMAKE_CURRENT_SOURCE_DIR}/libl5_api/libqos_client64.a
        IMPORTED_LOCATION_RELEASE ${CMAKE_CURRENT_SOURCE_DIR}/libl5_api/libqos_client64.a)
ENDIF()

ADD_LIBRARY(protobuf STATIC IMPORTED)
SET_TARGET_PROPERTIES(protobuf PROPERTIES
    IMPORTED_LOCATION ${EXTLIB_INSTALL_PREFIX}/lib/libprotobuf.a)

ADD_LIBRARY(boost_system STATIC IMPORTED)
SET_TARGET_PROPERTIES(boost_system PROPERTIES
    IMPORTED_LOCATION ${EXTLIB_INSTALL_PREFIX}/lib/libboost_system.a)

ADD_LIBRARY(boost_thread STATIC IMPORTED)
SET_TARGET_PROPERTIES(boost_thread PROPERTIES
    IMPORTED_LOCATION ${EXTLIB_INSTALL_PREFIX}/lib/libboost_thread.a
    IMPORTED_LINK_INTERFACE_LIBRARIES "boost_system;pthread;rt")

INCLUDE_DIRECTORIES("${GTEST_INC}")
ADD_LIBRARY(gtest STATIC IMPORTED)
SET_TARGET_PROPERTIES(gtest PROPERTIES
    IMPORTED_LOCATION ${GTEST_LIB}/libgtest.a
    IMPORTED_LINK_INTERFACE_LIBRARIES "pthread")
ADD_LIBRARY(gtest_main STATIC IMPORTED)
SET_TARGET_PROPERTIES(gtest_main PROPERTIES
    IMPORTED_LOCATION ${GTEST_LIB}/libgtest_main.a)

INCLUDE_DIRECTORIES("${GMOCK_INC}")
ADD_LIBRARY(gmock STATIC IMPORTED)
SET_TARGET_PROPERTIES(gmock PROPERTIES
    IMPORTED_LOCATION ${GMOCK_LIB}/libgmock.a
    IMPORTED_LINK_INTERFACE_LIBRARIES "pthread")
ADD_LIBRARY(gmock_main STATIC IMPORTED)
SET_TARGET_PROPERTIES(gmock_main PROPERTIES
    IMPORTED_LOCATION ${GMOCK_LIB}/libgmock_main.a)

